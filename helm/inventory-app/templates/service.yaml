apiVersion: v1
kind: Service
metadata:
  name: inventory-app
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8080
  selector:
    app: inventory-app